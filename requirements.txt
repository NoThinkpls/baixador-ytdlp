PySide6>=6.7
PySide6-Fluent-Widgets>=1.7
faster-whisper>=1.1,<2
# O motor de transcrição é o CTranslate2 (dependência do faster-whisper).
# O PyTorch NÃO é necessário: a detecção de CUDA usa ctranslate2 + driver.
# Em máquinas com GPU, o runtime instala nvidia-cublas-cu12 e nvidia-cudnn-cu12
# sob demanda, em vez das wheels CUDA completas do PyTorch.

PySide6>=6.7
PySide6-Fluent-Widgets>=1.7
faster-whisper>=1.1,<2
# O build instala a variante CUDA do PyTorch. Em execução, o app cai para CPU/int8
# quando CUDA não estiver disponível ou não puder ser inicializado.
torch>=2.5

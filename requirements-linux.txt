PySide6>=6.7
PySide6-Fluent-Widgets>=1.7
faster-whisper>=1.1,<2
# A primeira distribuição Linux usa CTranslate2 em CPU. CUDA continua opcional
# e não é empacotado para evitar uma imagem muito maior e dependente do driver.
